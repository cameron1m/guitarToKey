#!/usr/bin/env node
"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const commander_1 = __importDefault(require("commander"));
const path_1 = __importDefault(require("path"));
const child_process_1 = require("child_process");
const fs_extra_1 = __importDefault(require("fs-extra"));
const request_1 = require("../request");
const router_1 = require("../router");
const config_1 = require("../config");
const utils_1 = require("../utils");
function install(name, opts) {
    return __awaiter(this, void 0, void 0, function* () {
        if ((yield fs_extra_1.default.pathExists(name)) && (yield (yield fs_extra_1.default.stat(name)).isDirectory())) {
            yield upload(name, opts);
        }
        else {
            utils_1.logger.start(`fetching package info ${name}`);
            const params = {
                name,
                pyIndex: opts.tuna ? config_1.tunaMirrorURI : undefined
            };
            yield request_1.listen(`${router_1.route.plugin}/install`, params, {
                'info': (e) => {
                    const pkg = JSON.parse(e.data);
                    utils_1.logger.start(`installing ${pkg.name} from ${pkg.pipcook.source.uri}`);
                },
                'installed': (e) => {
                    const pkg = JSON.parse(e.data);
                    utils_1.logger.success(`${pkg.name} installed.`);
                },
                'error': (e) => {
                    utils_1.logger.fail(`install failed with ${e === null || e === void 0 ? void 0 : e.data}`);
                }
            });
        }
    });
}
function uninstall(name) {
    return __awaiter(this, void 0, void 0, function* () {
        utils_1.logger.start(`uninstalling ${name}`);
        yield request_1.get(`${router_1.route.plugin}/uninstall`, { name });
        utils_1.logger.success(`uninstalled ${name}`);
    });
}
function list(opts) {
    return __awaiter(this, void 0, void 0, function* () {
        const plugins = yield request_1.get(`${router_1.route.plugin}/list`, opts);
        if (plugins.length === 0) {
            console.info('no plugin installed.');
        }
        else {
            console.table(plugins, ['name', 'version', 'category', 'datatype']);
        }
    });
}
function upload(pathname, opts) {
    var _a, _b, _c;
    return __awaiter(this, void 0, void 0, function* () {
        try {
            const pkg = yield fs_extra_1.default.readJSON(path_1.default.join(pathname, 'package.json'));
            utils_1.logger.start(`installing ${pkg.name} from ${pathname}`);
            if (!(pkg === null || pkg === void 0 ? void 0 : pkg.pipcook)) {
                return utils_1.logger.fail('invalid plugin package');
            }
        }
        catch (err) {
            return utils_1.logger.fail(`read package.json error: ${err.message}`);
        }
        const params = {
            pyIndex: opts.tuna ? config_1.tunaMirrorURI : undefined
        };
        const output = child_process_1.spawnSync('npm', ['pack'], { cwd: pathname });
        let tarball;
        if (output.status !== 0) {
            return utils_1.logger.fail(output.stderr.toString());
        }
        else {
            utils_1.logger.info(output.stdout.toString());
            tarball = output.stdout.toString().replace(/[\n\r]/g, '');
        }
        const installingResp = yield request_1.uploadFile(`${router_1.route.plugin}/upload`, path_1.default.join(pathname, tarball), params);
        utils_1.logger.info(`installing plugin ${(_a = installingResp.data) === null || _a === void 0 ? void 0 : _a.name}@${(_b = installingResp.data) === null || _b === void 0 ? void 0 : _b.version}`);
        let errMsg;
        yield new Promise((resolve) => {
            request_1.listen(`${router_1.route.plugin}/log/${installingResp.data.logId}`, {}, {
                'log': (e) => {
                    const log = JSON.parse(e.data);
                    switch (log.level) {
                        case 'info':
                            utils_1.logger.info(log.data);
                            break;
                        case 'warn':
                            utils_1.logger.warn(log.data);
                            break;
                        default:
                            utils_1.logger.info(e.data.data);
                    }
                },
                'error': (e) => {
                    errMsg = e.data;
                    resolve();
                },
                'finished': resolve
            });
        });
        const pluginInfoResp = yield request_1.get(`${router_1.route.plugin}/${(_c = installingResp.data) === null || _c === void 0 ? void 0 : _c.id}`);
        if (typeof (pluginInfoResp === null || pluginInfoResp === void 0 ? void 0 : pluginInfoResp.id) === 'string') {
            utils_1.logger.info(`install plugin ${pluginInfoResp.name}@${pluginInfoResp.version} successfully`);
        }
        else {
            if (errMsg) {
                utils_1.logger.fail(`install plugin failed with error: ${errMsg}`);
            }
            else {
                utils_1.logger.fail('install plugin failed');
            }
        }
    });
}
commander_1.default
    .command('install <name>')
    .description('install the given plugin.')
    .option('--tuna', 'use tuna mirror to install python packages')
    .action((name, opts) => {
    if (name[0] === '.') {
        name = path_1.default.join(process.cwd(), name);
    }
    install(name, opts);
});
commander_1.default
    .command('uninstall <name>')
    .description('uninstall the given plugin')
    .action(uninstall);
commander_1.default
    .command('list')
    .description('list installed plugin')
    .option('-c|--category <name>', 'the plugin category')
    .option('-d|--datatype <name>', 'the plugin datatype')
    .action(list);
commander_1.default.parse(process.argv);
//# sourceMappingURL=pipcook-plugin.js.map